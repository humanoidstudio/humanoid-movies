%h1 Listing movies

%table
  %tr
    %th Name
    %th Published name
    %th Year
    %th Plot
    %th Imdb score
    %th Runtime
    %th
    %th
    %th

  - @movies.each do |movie|
    %tr
      %td= movie.name
      %td= movie.published_name
      %td= movie.year
      %td= movie.plot
      %td= movie.imdb_score
      %td= movie.runtime
      %td= link_to 'Show', movie
      %td= link_to 'Edit', edit_movie_path(movie)
      %td= link_to 'Destroy', movie, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Movie', new_movie_path
